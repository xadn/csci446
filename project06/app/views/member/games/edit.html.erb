<h1>Editing game</h1>

<% form_for([:member, @game]) do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Show', @game %> |
<%= link_to 'Back', member_games_path %>